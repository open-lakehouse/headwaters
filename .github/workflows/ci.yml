name: CI

on:
  merge_group:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel superseded runs on the same ref.
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

# Least privilege by default; jobs opt into more if they ever need it.
permissions: {}

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  CARGO_INCREMENTAL: "0"

jobs:
  # Detect whether a PR touches anything that affects the build. A docs-only
  # change (Markdown, docs/, repo-meta files) can't break compilation or move
  # coverage, so the expensive `test` and `coverage` jobs gate on this and skip
  # when `code == 'false'`. On push to main / merge_group there is no PR diff to
  # filter against, so the filter falls through and `code` is 'true' (everything
  # runs). fmt and clippy are cheap and always run regardless.
  changes:
    name: detect changes
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      pull-requests: read
    outputs:
      code: ${{ steps.filter.outputs.code }}
      proto: ${{ steps.filter.outputs.proto }}
      ui: ${{ steps.filter.outputs.ui }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36 # v3.0.2
        id: filter
        with:
          # `code` is true when ANY changed path is NOT one of these docs/meta
          # patterns (negations are anchored at the repo root). Keep this list in
          # sync with the set of files that genuinely can't affect the build.
          # `proto` gates the codegen-staleness check on changes to the .proto
          # sources, the buf config, or the committed generated output.
          filters: |
            code:
              - '!**/*.md'
              - '!docs/**'
              - '!LICENSE'
              - '!.gitignore'
              - '!.editorconfig'
            proto:
              - 'proto/**'
              - 'buf.yaml'
              - 'buf.gen.yaml'
              - 'buf.lock'
              - 'crates/headwaters/src/proto/**'
              - 'crates/headwaters/src/connect_gen/**'
            ui:
              - 'node/**'
              - 'crates/headwaters/ui.lock'

  # fmt, clippy, test and coverage are independent jobs that run in parallel.
  # Splitting them (rather than one sequential job) means a clippy or fmt failure
  # no longer prevents the test job from running — you still get full test
  # results on every push even when the lints are red. Wall-clock drops to the
  # slowest single job instead of the sum of all checks.

  fmt:
    name: fmt
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      # fmt only parses source, so it needs no protoc and no build cache —
      # keep this job seconds-fast for the quickest possible style feedback.
      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9 # master
        with:
          toolchain: "1.96"
          components: rustfmt

      - name: Format
        run: cargo fmt --all --check

  # Catch dangling dependencies (declared in Cargo.toml but never used). Cheap
  # and self-contained like fmt, so it always runs. Scoped to `crates/` — the
  # only Rust manifests; the action runs `cargo-machete` with no build needed.
  machete:
    name: machete
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - uses: bnjbvr/cargo-machete@ac30a525c0a8d163a92d727b3ff079ee3f6ecb08 # v0.9.2
        with:
          args: crates

  clippy:
    name: clippy
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      # clippy runs on a recent pinned stable for the best lints; the declared
      # MSRV floor (workspace rust-version) is verified separately by the `msrv`
      # job below.
      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9 # master
        with:
          toolchain: "1.96"
          components: clippy

      # cedar-policy's build script compiles .proto schemas via prost-build,
      # which shells out to `protoc`. Pin both the action (by commit SHA) and
      # the protoc version so the toolchain is reproducible; repo-token avoids
      # the unauthenticated GitHub API rate limit when resolving the release.
      - name: Install protoc
        uses: arduino/setup-protoc@c65c819552d16ad3c9b72d9dfd5ba5237b9c906b # v3.0.0
        with:
          version: "31.1"
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      # Caches ~/.cargo and ./target keyed on the lockfile. clippy and test share
      # the "ci" shared-key so they reuse one warmed target/; only main writes the
      # cache (save-if) so parallel PR jobs don't thrash each other's entry.
      - uses: Swatinem/rust-cache@98c8021b550208e191a6a3145459bfc9fb29c4c0 # v2.8.0
        with:
          shared-key: ci
          save-if: ${{ github.ref == 'refs/heads/main' }}

      # The same gate enforced locally: deny all warnings across every target and
      # feature (incl. `governance` and the opt-in `tpcds` bench scaffolding).
      - name: Clippy
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

  # Verify the workspace actually builds on its declared MSRV floor
  # (rust-version in the workspace Cargo.toml). The other jobs run on a recent
  # pinned stable, so without this nothing would catch a dependency or syntax
  # bump that quietly raises the real minimum below what we promise on docs.rs.
  msrv:
    name: msrv
    # Skip on docs-only PRs (no code change can move the build result).
    needs: changes
    if: ${{ needs.changes.outputs.code == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      # Pinned to the workspace's declared rust-version. Bump in lock-step with
      # Cargo.toml's `rust-version` so this job tests exactly what we promise.
      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9 # master
        with:
          toolchain: "1.91"

      - name: Install protoc
        uses: arduino/setup-protoc@c65c819552d16ad3c9b72d9dfd5ba5237b9c906b # v3.0.0
        with:
          version: "31.1"
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - uses: Swatinem/rust-cache@98c8021b550208e191a6a3145459bfc9fb29c4c0 # v2.8.0
        with:
          shared-key: msrv
          save-if: ${{ github.ref == 'refs/heads/main' }}

      - name: Build on MSRV
        run: cargo build --workspace --all-features --locked

  # The generated proto + ConnectRPC code under crates/headwaters/src/
  # {proto,connect_gen}/ is committed (so the workspace builds without a codegen
  # step). Regenerate here and fail if the committed output drifts from the
  # .proto sources — a `proto/` edit must land with its regenerated output.
  proto-check:
    name: proto-check
    needs: changes
    if: ${{ needs.changes.outputs.proto == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - uses: bufbuild/buf-setup-action@a47c93e0b1648d5651a065437926377d060baa99 # v1.50.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9 # master
        with:
          toolchain: "1.96"
          components: rustfmt

      # The only local codegen plugin `buf generate` invokes (the buffa and
      # connect-rust passes run remotely on the BSR and need no install).
      - name: Install packaging codegen plugin
        run: cargo install protoc-gen-buffa-packaging

      - name: Regenerate proto code
        run: buf generate

      - name: Format generated code
        run: cargo fmt -p headwaters

      # `just proto-gen` is exactly the two steps above; fail if anything moved.
      - name: Check committed output is up to date
        run: |
          if ! git diff --quiet; then
            echo "::error::Generated proto code is stale. Run \`just proto-gen\` and commit the result."
            git diff --stat
            exit 1
          fi

  # The bundled lineage UI (node/) ships inside the headwaters Docker image but
  # lives outside the crate, so release-plz can't see UI changes. crates/
  # headwaters/ui.lock is a fingerprint of node/ that lives in the crate; a UI
  # change must land with its regenerated lock so release-plz bumps headwaters
  # and the Docker image rebuilds. Fail if the committed lock is stale — same
  # "regenerate and diff" pattern as proto-check. See `just ui-fingerprint`.
  ui-fingerprint:
    name: ui-fingerprint
    needs: changes
    if: ${{ needs.changes.outputs.ui == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - uses: extractions/setup-just@e33e0265a09d6d736e2ee1e0eb685ef1de4669ff # v3.0.0

      - name: Check UI fingerprint is up to date
        run: just ui-fingerprint-check

  test:
    name: test
    # Skip on docs-only PRs (no code change can move test results).
    needs: changes
    if: ${{ needs.changes.outputs.code == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      # dorny/test-reporter publishes a check run with the parsed JUnit results.
      checks: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9 # master
        with:
          toolchain: "1.96"

      - name: Install protoc
        uses: arduino/setup-protoc@c65c819552d16ad3c9b72d9dfd5ba5237b9c906b # v3.0.0
        with:
          version: "31.1"
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - uses: Swatinem/rust-cache@98c8021b550208e191a6a3145459bfc9fb29c4c0 # v2.8.0
        with:
          shared-key: ci
          save-if: ${{ github.ref == 'refs/heads/main' }}

      # Prebuilt binary (no compile); the ci profile emits JUnit XML.
      - uses: taiki-e/install-action@8b3c737da4b541bf0fb5a3e0488ff20535badac9 # v2.82.1
        with:
          tool: cargo-nextest

      # Live-integration tests (UC / OCI registry / a running hydrofoil) are
      # marked #[ignore], so the default run needs no services. The always-on
      # OpenLineage spec-conformance suite (tests/conformance.rs) runs here too —
      # it validates emitted events against vendored JSON Schemas, no services.
      # --profile ci writes target/nextest/ci/junit.xml and runs every test
      # (fail-fast = false) so the report is complete even on failure.
      - name: Test
        run: cargo nextest run --workspace --all-features --profile ci

      # nextest does not run doctests; the old `cargo test` did, so run them here
      # to keep doctest coverage. Runs even if nextest failed above.
      - name: Doctests
        if: ${{ !cancelled() }}
        run: cargo test --workspace --all-features --doc

      # Surface per-test pass/fail in the PR Checks tab regardless of exit code —
      # this is what gives you results even when clippy/fmt are failing.
      - name: Publish test report
        uses: dorny/test-reporter@a43b3a5f7366b97d083190328d2c652e1a8b6aa2 # v3.0.0
        if: ${{ !cancelled() }}
        with:
          name: nextest results
          path: target/nextest/ci/junit.xml
          reporter: jest-junit

      - name: Upload JUnit artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: ${{ !cancelled() }}
        with:
          name: junit-results
          path: target/nextest/ci/junit.xml
          if-no-files-found: warn

  coverage:
    name: coverage
    # Skip on docs-only PRs (no code change can move coverage).
    needs: changes
    if: ${{ needs.changes.outputs.code == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      # Doctest coverage (`--doc` / `report --doctests`) relies on
      # `-Z unstable-options --persist-doctests`, which only the nightly
      # compiler accepts. Install nightly so the doctest coverage step can run
      # via `cargo +nightly`. Stable is installed last below so it stays the
      # default toolchain for the nextest run.
      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9 # master
        with:
          toolchain: nightly
          components: llvm-tools-preview

      # llvm-tools-preview ships the profdata tooling cargo-llvm-cov needs.
      # Installed after nightly so "1.96" remains the default toolchain.
      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9 # master
        with:
          toolchain: "1.96"
          components: llvm-tools-preview

      - name: Install protoc
        uses: arduino/setup-protoc@c65c819552d16ad3c9b72d9dfd5ba5237b9c906b # v3.0.0
        with:
          version: "31.1"
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      # Coverage recompiles with -C instrument-coverage, so it cannot reuse the
      # test/clippy "ci" cache — give it its own shared-key.
      - uses: Swatinem/rust-cache@98c8021b550208e191a6a3145459bfc9fb29c4c0 # v2.8.0
        with:
          shared-key: coverage
          save-if: ${{ github.ref == 'refs/heads/main' }}

      - uses: taiki-e/install-action@8b3c737da4b541bf0fb5a3e0488ff20535badac9 # v2.82.1
        with:
          tool: cargo-llvm-cov,cargo-nextest

      # Collect coverage from both nextest tests and doctests into one report.
      # Doctest coverage needs nightly (unstable `--persist-doctests`); the
      # nextest run stays on the pinned stable toolchain.
      #
      # `--ignore-filename-regex` drops code we never want counted from the report
      # itself, so `codecov.json` never carries it:
      #   * committed codegen — the buffa-generated proto message types
      #     `src/proto/*.v1.rs` and the connectrpc stubs under `src/connect_gen/`
      #     (the hand-written `src/proto/mod.rs` is deliberately NOT matched);
      #   * `src/main.rs` — process entry-point wiring no in-process test drives;
      #   * `src/projection/applier.rs` — a contract trait with no direct callers.
      # codecov.yml's `ignore` does the same server-side; filtering at the source
      # keeps the uploaded artifact honest too. Keep the two in sync.
      - name: Collect coverage
        run: |
          cargo llvm-cov --no-report nextest --workspace --all-features
          cargo +nightly llvm-cov --no-report --doc --workspace --all-features
          cargo +nightly llvm-cov report --doctests --codecov --output-path codecov.json \
            --ignore-filename-regex 'headwaters/src/(proto/[^/]+\.v1\.rs|connect_gen/|main\.rs|projection/applier\.rs)'

      # Codecov rejects tokenless uploads for this repo
      # ("Token required - not valid tokenless upload"), so authenticate with
      # the repository upload token stored as the CODECOV_TOKEN secret. Never
      # fails CI on a flaky upload, and codecov.yml keeps the status
      # informational so it can't block a PR.
      - name: Upload to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          files: codecov.json
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}

  # The OpenLineage adapter's acceptance test against the Marquez reference
  # backend. Separate job: it needs Docker (testcontainers spins up Marquez +
  # Postgres) and is gated behind the `marquez-it` feature + #[ignore], so it
  # never runs in the authoritative `ci` job above. Non-blocking for now
  # (continue-on-error) while the live harness beds in; flip off once stable.
  marquez-acceptance:
    name: marquez reference acceptance
    # Skip on docs-only PRs — no code change to validate against Marquez.
    needs: changes
    if: ${{ needs.changes.outputs.code == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    continue-on-error: true
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9 # master
        with:
          toolchain: "1.96"

      - name: Install protoc
        uses: arduino/setup-protoc@c65c819552d16ad3c9b72d9dfd5ba5237b9c906b # v3.0.0
        with:
          version: "31.1"
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - uses: Swatinem/rust-cache@98c8021b550208e191a6a3145459bfc9fb29c4c0 # v2.8.0

      # ubuntu-latest ships a working Docker daemon; testcontainers finds it at
      # the default socket. The test pulls marquezproject/marquez + postgres.
      - name: Marquez acceptance test
        run: cargo test -p datafusion-openlineage --features marquez-it --test marquez_acceptance -- --ignored --nocapture

  # Single roll-up status. Because `test`/`coverage` are conditionally skipped on
  # docs-only PRs, the individual checks aren't a stable name to require in branch
  # protection. This job always runs and turns red only if a job that *did* run
  # failed — a skipped job is a pass. Make THIS the one required check, so docs
  # PRs stay green without leaving a required check forever "pending".
  ci:
    name: ci
    if: ${{ always() }}
    needs: [fmt, machete, clippy, msrv, proto-check, ui-fingerprint, test, coverage]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions: {}
    steps:
      - name: Verify required jobs succeeded or were skipped
        run: |
          # `needs.*.result` is one of: success | failure | cancelled | skipped.
          # Skipped is acceptable (docs-only path filtering); anything else that
          # isn't success fails the gate.
          for r in \
            "fmt:${{ needs.fmt.result }}" \
            "machete:${{ needs.machete.result }}" \
            "clippy:${{ needs.clippy.result }}" \
            "msrv:${{ needs.msrv.result }}" \
            "proto-check:${{ needs.proto-check.result }}" \
            "ui-fingerprint:${{ needs.ui-fingerprint.result }}" \
            "test:${{ needs.test.result }}" \
            "coverage:${{ needs.coverage.result }}"; do
            name="${r%%:*}"; result="${r##*:}"
            echo "$name -> $result"
            if [ "$result" != "success" ] && [ "$result" != "skipped" ]; then
              echo "::error::required job '$name' did not succeed ($result)"
              exit 1
            fi
          done
          echo "All required jobs passed (or were skipped)."
